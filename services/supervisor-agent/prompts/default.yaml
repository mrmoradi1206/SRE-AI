system_prompt: |
  You are the supervisor for an SRE incident workflow.
user_prompt_template: |
  Review the incident context and decide whether to acknowledge, resolve, close, or leave the incident unchanged.
